import { measure } from "../utils/utils";
import { Day } from "../utils/day";

export class Solution extends Day {
  
  @measure
  prepData(data: string[]): void {
  }

  @measure
  task1() {
    this.logAnswer(1, null);
  }

  @measure
  task2() {
    this.logAnswer(2, null);
  }
}
